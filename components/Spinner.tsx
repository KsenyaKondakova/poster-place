import React from 'react';
import { ScaleLoader } from 'react-spinners';

function Spinner() {
  return <ScaleLoader color="#2a2a2a" height={20} margin={2} radius={1} width={4} />;
}

export default Spinner;
